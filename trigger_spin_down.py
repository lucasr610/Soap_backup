#!/usr/bin/env python3
"""
Author: Lucas Reynolds <lucasreynolds1988@gmail.com>
License: MIT
Generated by: Codex God Mode ∞ — ARCH Version
"""
"""Spin-Down trigger wrapper."""
import logging
import subprocess
from pathlib import Path

from codex_controller import get_soap_root

LOG_PATH = get_soap_root() / "logs" / "trigger_spin_down.log"
LOG_PATH.parent.mkdir(parents=True, exist_ok=True)
logging.basicConfig(filename=str(LOG_PATH), level=logging.INFO,
                    format="%(asctime)s [%(levelname)s] %(message)s")
logger = logging.getLogger(__name__)


def main() -> None:
    logger.info("Spin-Down initiated from trigger")
    root = get_soap_root()
    subprocess.run(["python3", str(root / "spin_down.py")], check=False)
    logger.info("Spin-Down complete")


if __name__ == "__main__":
    main()
